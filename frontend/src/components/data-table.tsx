export function DataTable({columns,rows}:{columns:Array<{key:string;label:string}>,rows:Array<Record<string,unknown>>}){return <div style={{overflowX:"auto"}}><table><thead><tr>{columns.map(c=><th key={c.key} scope="col">{c.label}</th>)}</tr></thead><tbody>{rows.length?rows.map((row,i)=><tr key={String(row.id??i)}>{columns.map(c=><td key={c.key}>{String(row[c.key]??"")}</td>)}</tr>):<tr><td colSpan={columns.length}>No records yet.</td></tr>}</tbody></table></div>}
