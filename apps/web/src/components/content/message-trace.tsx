"use client";import { useEffect,useState } from "react";import { phase1Api } from "../../lib/phase1-api";
export function MessageTrace({workspaceId,messageId}:{workspaceId:string;messageId:string}){const [data,setData]=useState<any>(null),[error,setError]=useState("");useEffect(()=>{phase1Api(`/api/v1/workspaces/${workspaceId}/messages/${messageId}/trace`).then(setData).catch(e=>setError(e.message))},[workspaceId,messageId]);return <section><h1>Message trace</h1>{error&&<div className="panel">{error}</div>}{data&&<><div className="panel"><p><strong>State:</strong> {data.message.state}</p><p><strong>Profile:</strong> {data.message.profileId}</p><p><strong>Email version:</strong> {data.message.emailVersionId}</p></div><div className="panel"><h2>Timeline</h2>{data.events.map((e:any,i:number)=><div key={i}><strong>{e.kind}</strong> <small>{new Date(e.occurredAt).toLocaleString()}</small><pre>{JSON.stringify(e.detail,null,2)}</pre></div>)}</div></>}</section>}
