"use client";
export function ConfirmationDialog({open,title,consequence,onConfirm,onCancel,confirmLabel="Confirm",pending=false,pendingLabel="Saving…",error,tone="default"}:{open:boolean;title:string;consequence:string;onConfirm:()=>void;onCancel:()=>void;confirmLabel?:string;pending?:boolean;pendingLabel?:string;error?:string;tone?:"default"|"danger"}){if(!open)return null;return <div className="modal-backdrop" role="presentation"><section className="modal confirmation-dialog" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><h2 id="confirm-title">{title}</h2><p>{consequence}</p>{error && <p role="alert">{error}</p>}<footer><button type="button" className="button-secondary" disabled={pending} onClick={onCancel}>Cancel</button><button type="button" className={tone==="danger"?"button-danger":"button-primary"} disabled={pending} onClick={onConfirm}>{pending?pendingLabel:confirmLabel}</button></footer></section></div>}
