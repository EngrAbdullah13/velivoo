import test from 'node:test';import assert from 'node:assert/strict';import {hasPermission} from '../packages/domain/src/phase1/permissions.js';
test('Phase3 uses discrete flow activation and API-key permissions',()=>{assert.equal(hasPermission('owner','flows.activate'),true);assert.equal(hasPermission('admin','flows.activate'),true);assert.equal(hasPermission('marketer','flows.write'),true);assert.equal(hasPermission('marketer','flows.publish'),true);assert.equal(hasPermission('marketer','flows.activate'),false);assert.equal(hasPermission('analyst','flows.read'),true);assert.equal(hasPermission('analyst','flows.write'),false);assert.equal(hasPermission('marketer','api_keys.manage'),false);assert.equal(hasPermission('admin','api_keys.manage'),true)});
